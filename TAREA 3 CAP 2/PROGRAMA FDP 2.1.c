#include <stdio.h>

/* Promedio curso
El programa, al recibir como dato el promedio de un alumno e un curso
universitario, escribe aprobadosi su promedio es mayor o igual a 6.

PRO: variable de tipo real. */

void mai (void)
{
    float Pro;
    printf ("ingrese el promedio del alumno: ");
    scanf("%f", &PRO);
    if (PRO >= 6)
        printf("\nAprobado");
}
